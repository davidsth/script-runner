# frozen_string_literal: true

RSpec.describe ScriptRunner do
  it "has a version number" do
    expect(ScriptRunner::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
